en:
  vcloud:
    creating_cloned_vm: |-
      Calling vCloud CloneVM with the following settings:
    destroy_vm: |-
      Calling vCloud Destroy
    power_off_vm: |-
      Calling vCloud PowerOff
    vm_already_created: |-
      The VM is already created
    vm_not_created: |-
      The VM has not been created
  errors:
    missing_template: |-
      Configured template VM could not be found
    missing_datacenter: |-
      Configured data center not found
    missing_compute_resource: |-
      Configured compute resource not found
    missing_resource_pool: |-
      Configured resource pool not found
  config:
    hostname: |-
      Configuration must specify a vCloud Director hostname
    username: |-
      Configuration must specify a vCloud Director username
    password: |-
      Configuration must specify a vCloud Director password
    orgname: |-
      Configuration must specify a vCloud Director organization
    api_version: |-
      Configuration must specify a vCloud API version (default=5.1)
    catalog_name: |-
      Configuration must specify a vCloud Director Catalog (for the VM templates images)
    vdc_name: |-
      Configuration must specify a vCloud Director Virtual Datacenter (Target Organization VDC)
    name: |-
      Configuration must specify a VM name
    template: |-
      Configuration must specify a template name
    compute_resource: |-
      Configuration must specify a compute resource name
    resource_pool: |-
      Configuration must specify a resource pool name
  states:
    short_running: |-
      VM is running
    short_stopped: |-
      VM is stopped
    short_suspended: |-
      VM is suspended
    short_notcreated: |-
      VM is not created yet




